# NexusOne UI

Next.js 14 app on port **3005**.

```bash
cp ../.env.example .env.local
npm install
npm run dev
```

`NEXT_PUBLIC_API_URL=http://localhost:8085/api/v1`  
`NEXT_PUBLIC_WS_URL=ws://localhost:8085/ws/incidents`

Session token key: `nexusone.token`.
